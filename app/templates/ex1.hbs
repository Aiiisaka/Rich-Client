{{page-title "Ex1"}}
<div class="card">
  <div class="card-header bg-primary text-light">Ex1 : Note application</div>
  <div class="card-body">
    <Textarea class="form-control" @value={{this.content}} @input={{this.update}} maxlength="100" minlength="0"></Textarea>
    <br>
    <button type="button" class="btn btn-primary btn-lg" {{action "save"}}>Sauvegarder</button>
    <button type="button" class="btn btn-secondary btn-lg" {{action "clear"}}>Effacer</button>
    <br><br>
    <div class="alert {{this.status}}">Il reste {{this.size}} caractères disponibles.</div>
    <div class="alert {{this.style}}">{{this.info}}</div>
  </div>
</div>
{{outlet}}